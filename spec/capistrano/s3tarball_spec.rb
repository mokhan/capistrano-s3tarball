require "spec_helper"

describe Capistrano::S3tarball do
  it "has a version number" do
    expect(Capistrano::S3tarball::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to be_falsey
  end
end
